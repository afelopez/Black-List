<h1>Listing reports</h1>

<table>
  <tr>
    <th>Summoner</th>
    <th>Type</th>
    <th>Reason</th>
    <th>Detail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reports.each do |report| %>
  <tr>
    <td><%= report.summoner %></td>
    <td><%= report.types %></td>
    <td><%= report.reason %></td>
    <td><%= report.detail %></td>
    <td><%= link_to 'Show', report %></td>
    <td><%= link_to 'Edit', edit_report_path(report) %></td>
    <td><%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />


